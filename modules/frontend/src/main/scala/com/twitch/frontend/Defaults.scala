package com.twitch.frontend

import scala.concurrent.duration.*

object Defaults:
  val NotificationHistoryLimit = 50
  val SearchPageSize = 5
  val TagsPerNotificationCard = 3
  val SseReconnectDelay = 5.seconds
